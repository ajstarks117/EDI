{
  "name": "traveltrek-blockchain",
  "version": "1.0.0",
  "description": "Smart Contracts and blockchain identity management for TravelTrek",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "node": "npx hardhat node",
    "deploy:local": "npx hardhat run scripts/deploy.js --network localhost"
  },
  "dependencies": {
    "dotenv": "^16.4.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.5"
  }
}
